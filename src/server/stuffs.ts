export const counter = <T>(arr: T[]): string => `There are ${arr.length} elements in this array`;

export const adder = (a: number, b: number): string => `The sum of ${a} and ${b} is ${a + b}`;

export const pi: number = 3.142;
